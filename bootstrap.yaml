# Save this on your laptop as master-app.yaml and run: kubectl apply -f master-app.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-bootstrap
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/ok-karthik/platform-engineering-idp-gitops-reference-architecture.git"
    targetRevision: HEAD
    path: platform-charts # This tells Argo to apply everything inside the bootstrap folder!
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - SkipDryRunOnMissingResource=true
