replicaCount: 1

image:
  repository: "ghcr.io///demo-tenant/my-python-app"
  pullPolicy: IfNotPresent
  tag: "latest"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

cloud_services: ['aws-s3', 'aws-postgres']

serviceAccount:
  create: true
  annotations: {}
  name: ""

podAnnotations:
  instrumentation.opentelemetry.io/inject-python: "my-python-app-autoinstrumentation"

podLabels: 
  team: "demo-tenant"

podSecurityContext: {}
securityContext: {}

service:
  type: ClusterIP
  port: 8080

resources: {}

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 20
  targetCPUUtilizationPercentage: 80

nodeSelector: {}
tolerations: []
affinity: {}

ingress:
  enabled: true
  annotations: {}
  hosts:
    - host: "my-python-app.demo-tenant.localhost"
      paths:
        - path: /
          pathType: Prefix
